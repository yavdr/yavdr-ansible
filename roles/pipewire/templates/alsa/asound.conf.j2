{{ ansible_managed.format(file=template_path) | comment }}
# explicit config for pipewire
pcm.!default {
  type pipewire
  hint {
    show on
    description "Default ALSA Output (PipeWire Sound Server)"
  }
}

ctl.!default {
  type pipewire
}